import {
  headers,
  requestLimit,
  rateLimit
} from "./middleware";

const fortress = {
  headers,
  requestLimit,
  rateLimit
};

export default fortress;

export {
  headers,
  requestLimit,
  rateLimit
};